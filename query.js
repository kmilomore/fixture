const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { try { const disciplines = await prisma.discipline.findMany(); const categories = await prisma.category.findMany(); console.log("Disciplines:"); console.table(disciplines, ["name"]); console.log("Categories:"); console.table(categories, ["name", "gender"]); } catch (e) { console.error(e); } finally { await prisma.$disconnect(); } } main();
